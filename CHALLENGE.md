# fantuan 挑战

三个挑战，随便挑，奖品一样。用这个专门折磨人的语言做点真能玩的东西。

## 通用规则（先读）

1. 必须用 fantuan v3 编写：**单个** `.femboy` 源文件，能通过 `fantuan build` 编译成原生 ELF。
2. 纯 syscall、无 libc；不允许调用外部程序、脚本、现成游戏/图形库。
3. 提交方式（**走 GitHub Issues，别发邮件**）：
   - 先在自己的 GitHub 仓库里放这个单文件源码（只放源码和说明，**不要放任何二进制**，防止病毒）；
   - 然后到 <https://github.com/fantuanmtf/fantuan/issues/new> 提 Issue；
   - 标题格式：`[challenge/pacman]`、`[challenge/calc]`、`[challenge/parkour]` 三选一；
   - 内容：仓库链接 + 编译/运行说明 + 可选截图。
4. 每个挑战的**第一位通过者**获得奖励：作者女装照一张（作者承诺**不露脸**；照片可自行用 `exiftool` 清除元数据）。后来者没有奖品，但可以换个挑战继续。
5. 作者会拉源码检查并实际编译运行，跑不通不算通过。

## 挑战一：彩色 2D 三关吃豆人

- 彩色 2D 的吃豆人，**三个关卡**。
- 迷宫、吃豆人、豆子、至少一只幽灵、得分、吃豆/被抓判定、通关与失败判定。
- 三关要有可区分的差异（地图、速度、幽灵数量随你）。
- 键盘控制；彩色用终端 ANSI 或直接写 `/dev/fb0`，自己挑一个受得住的。

## 挑战二：加减乘除计算器

- 终端里输入，支持 `+ - * /`。
- 支持多位数和负数。
- 算完能接着算（连续运算，输入 `q` 退出）。
- 除以零必须拒绝并提示，不许崩、不许输出垃圾结果。
- 结果算错就没有然后了。

## 挑战三：2D 平面火柴人跑酷

- 彩色 2D，火柴人自动向一侧跑（终端 ANSI 或 `/dev/fb0`）。
- 火柴人要用基本图形画出来：头、身体、手、腿，别拿一个 `@` 糊弄。
- 跳跃 + 重力下落，键盘控制。
- 障碍物、碰撞判定、距离/得分显示，撞上就结束。
- 速度随距离加快（可选，但不加也拦不住别人加）。

## 提示（不算作弊）

- 先想清楚每帧画什么，再想怎么把手写机器码的偏移算对。
- 终端彩色只要 `\033[38;5;Nm`，难的是算偏移，不是算颜色。
- 每次改完记得重算 `;CK=`，这正是这个语言的精髓。
